#!/usr/bin/python3
def complex_delete(a_dictionary, value):
    while val in a_dictionary.values():
        for l, m in a_dictionary.items():
            if m == value:
                del a_dictionary[l]
                break

    return (a_dictionary)
